<Window x:Class="Nexus.App.DriveChooserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Choose what to scan"
        Height="520" Width="620"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="{StaticResource BgBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Which drives should Nexus scan?" Style="{StaticResource H1}"/>

        <TextBlock Grid.Row="1" Style="{StaticResource DimText}" Margin="0,0,0,16"
                   Text="Tick the drives you want checked. A drive with a lot on it can take a while — you can stop at any point and keep whatever was found."/>

        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="8">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="DriveList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="10,8" Margin="0,2" CornerRadius="6"
                                    Background="{StaticResource PanelAltBrush}">
                                <CheckBox IsChecked="{Binding Selected, Mode=TwoWay}" VerticalContentAlignment="Center">
                                    <StackPanel Margin="8,0,0,0">
                                        <TextBlock Text="{Binding Display}" FontWeight="SemiBold" FontSize="15"/>
                                        <TextBlock Style="{StaticResource DimText}" FontSize="13">
                                            <Run Text="{Binding Kind, Mode=OneWay}"/>
                                            <Run Text=" · "/>
                                            <Run Text="{Binding Size, Mode=OneWay}"/>
                                        </TextBlock>
                                    </StackPanel>
                                </CheckBox>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <DockPanel Grid.Row="3" Margin="0,16,0,0">
            <Button DockPanel.Dock="Right" Content="Start scan" Style="{StaticResource PrimaryButton}"
                    IsDefault="True" Click="OnStart" Margin="8,0,0,0"/>
            <Button DockPanel.Dock="Right" Content="Cancel" Style="{StaticResource QuietButton}"
                    IsCancel="True" Click="OnCancel" Margin="0"/>
            <TextBlock x:Name="Summary" Style="{StaticResource DimText}" VerticalAlignment="Center"/>
        </DockPanel>
    </Grid>
</Window>
